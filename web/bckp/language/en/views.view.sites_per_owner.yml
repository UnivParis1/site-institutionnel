display:
  default:
    display_options:
      exposed_form:
        options:
          reset_button_label: Reset
          exposed_sorts_label: 'Sort by'
      arguments:
        user_id:
          exception:
            title: All
  from_route:
    display_options:
      arguments:
        user_id:
          exception:
            title: All
