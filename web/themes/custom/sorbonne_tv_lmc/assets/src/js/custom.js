/*
(function ($, Drupal, once) {
    Drupal.behaviors.sociallink = {
        attach: function (context, settings) {
            once('myBehavior', 'html').forEach(function (element) {
                myFunction(element);
            });
        }
    };
})(jQuery, Drupal, once);
*/
