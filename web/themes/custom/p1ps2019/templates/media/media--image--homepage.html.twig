{#
/**
 * @file
 * Theme override to display a media item.
 *
 * Available variables:
 * - name: Name of the media.
 * - content: Media content.
 *
 * @see template_preprocess_media()
 *
 * @ingroup themeable
 */
#}
{% if content.field_credits|render %}
{% endif %}
{% if content.field_legende|render %}

{% endif %}
  {% if content %}
    <figure>
      {{ content.field_media_image }}
      {% if content.field_credits|render or content.field_legende|render %}
        <figcaption class="field field--name-field-credits field--type-string field--label-hidden field--item">
          {% if content.field_credits|render %}
            {{ content.field_credits.0 }}
          {% endif %}
          {% if content.field_credits|render and content.field_legende|render%}
            |
          {% endif %}
          {% if content.field_legende|render %}
            {{ content.field_legende.0 }}
          {% endif %}
        </figcaption>
      {% endif %}
  </figure>
  {% endif %}
